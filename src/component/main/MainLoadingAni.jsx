import styled from 'styled-components';

const MainLoadingAni = styled.div`
  width: 50%;
  margin: auto;
  height: 50vh;
  background-image: url('./image/mainLoading.gif');
`;

export default MainLoadingAni;
